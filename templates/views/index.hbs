<!DOCTYPE html>
<html>

<head>
    <title>{{{title}}}</title>
</head>

<body>
    {{!--
    <script src="{{{baseUrl}}}/fire.app/{{{fireAppVersion}}}/index.js"></script> --}}
    {{!--
    <script>
        fireApp({
            apps: {{{ apps }}},
            navigations: {{{ navigations }}},
            config: {{{ config }}},
            features: {{{ features }}},
        })
    </script> --}}
    <div id="root"></div>
    <h1>Hello from Dummy! Sorry, FireApp doesn't work.</h1>
</body>

</html>
